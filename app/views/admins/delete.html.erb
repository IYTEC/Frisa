<% @page_title = "Delete Admin User: #{@admin.f_name}" %>

<%= link_to("<< Back to Menu", {:controller => "access", :action => "index"}) %><br>
<%= link_to("<< Back to List", {:action => "index"}) %>

<h2>Delete Admin User</h2>
<%= form_for(:admin, :url => {:action => "destroy", :id => @admin.id}) do |f| %>
	<table>
		<tr>
			<p> Are you sure you want to delete this Admin <span style="font-size: 150%"><%= @admin.f_name %></span></p>
			
		</tr>		
	</table>
	<%= submit_tag("Delete Admin") %>
<% end %>