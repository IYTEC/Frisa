<!DOCTYPE html>
<html>
<head>
  <title>Frisa | <%= @page_title || "Admin" %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <% if !flash[:notice].blank? %>
  <p><%= flash[:notice] %></p>
<% end %>
<nav class="nav-style navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button style="border-radius: 0;" type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand"  style="font-size: 20px" href="#">Brand</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="dropdown">
              <a class="dropdown-toggle " style="font-size: 17px" type="submit" data-toggle="dropdown"><span class="fa fa-bars" > | <span style="font-family: sans-serif;">MENU</span></span></a>
              <ul class="dropdown-menu">
                <% if session[:username] %>
                  <li><%= link_to("Logout", :controller => "access", :action => "logout") %></li>
                <% else %>
                  <li><%= link_to("Logout", :controller => "access", :action => "logout") %></li>
                <% end %>
              </ul>
        </li>
      </ul>
      <form class="navbar-form navbar-right" role="search">
        <div class="form-group">
          <input style="border: solid 0.5%; border-radius:0;" type="text" class="form-control" placeholder="Search">
        </div>
        <button style="border-radius: 0; border: solid 0.5%;" type="submit" class="btn btn-default">Submit</button>
      </form>
    </div><!-- /.navbar-collapse -->
    <p>Logged in as: <%= session[:username] || "Guest" %></p>

  </div><!-- /.container-fluid -->
</nav>

<%= yield %>


</body>
</html>
